# Required if you use any Gemini model
GEMINI_API_KEY=your_gemini_api_key_here

# Optional providers (required only when selected by model/provider config)
GROQ_API_KEY=your_groq_api_key_here
CEREBRAS_API_KEY=your_cerebras_api_key_here

# Provider routing (defaults are controlled in code)
# LLM_PROVIDER=groq

# Optional Gemini model overrides (defaults shown)
# GEMINI_FLASH_MODEL=gemini-3-flash-preview
# GEMINI_PRO_MODEL=gemini-3-pro

# Optional session limits
# MAX_ITERATIONS=20
# MAX_COST_USD=5.0
# MAX_RUNTIME_SECONDS=600

# Optional CORS (for split frontend/backend deployments)
# CORS_ALLOW_ORIGINS=https://your-frontend.example.com,http://localhost:5173
# CORS_ALLOW_ORIGIN_REGEX=
